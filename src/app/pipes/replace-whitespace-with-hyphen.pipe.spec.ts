import { ReplaceWhitespaceWithHyphenPipe } from './replace-whitespace-with-hyphen.pipe';

describe('ReplaceWhitespaceWithHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWhitespaceWithHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
